shadow$provide.module$node_modules$monaco_editor$esm$vs$base$browser$ui$aria$aria = function(require, module, exports) {
  function setARIAContainer(parent) {
    ariaContainer = document.createElement("div");
    ariaContainer.className = "monaco-aria-container";
    var createAlertContainer = () => {
      const element = document.createElement("div");
      element.className = "monaco-alert";
      element.setAttribute("role", "alert");
      element.setAttribute("aria-atomic", "true");
      ariaContainer.appendChild(element);
      return element;
    };
    alertContainer = createAlertContainer();
    alertContainer2 = createAlertContainer();
    createAlertContainer = () => {
      const element = document.createElement("div");
      element.className = "monaco-status";
      element.setAttribute("aria-live", "polite");
      element.setAttribute("aria-atomic", "true");
      ariaContainer.appendChild(element);
      return element;
    };
    statusContainer = createAlertContainer();
    statusContainer2 = createAlertContainer();
    parent.appendChild(ariaContainer);
  }
  function alert(msg) {
    ariaContainer && (alertContainer.textContent !== msg ? (dom.clearNode(alertContainer2), insertMessage(alertContainer, msg)) : (dom.clearNode(alertContainer), insertMessage(alertContainer2, msg)));
  }
  function status(msg) {
    ariaContainer && (statusContainer.textContent !== msg ? (dom.clearNode(statusContainer2), insertMessage(statusContainer, msg)) : (dom.clearNode(statusContainer), insertMessage(statusContainer2, msg)));
  }
  function insertMessage(target, msg) {
    dom.clearNode(target);
    msg.length > 20000 && (msg = msg.substr(0, 20000));
    target.textContent = msg;
    target.style.visibility = "hidden";
    target.style.visibility = "visible";
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, alert:{enumerable:!0, get:function() {
    return alert;
  }}, setARIAContainer:{enumerable:!0, get:function() {
    return setARIAContainer;
  }}, status:{enumerable:!0, get:function() {
    return status;
  }}});
  var dom = require("module$node_modules$monaco_editor$esm$vs$base$browser$dom");
  require("module$node_modules$monaco_editor$esm$vs$base$browser$ui$aria$aria_css");
  let ariaContainer, alertContainer, alertContainer2, statusContainer, statusContainer2;
};

//# sourceMappingURL=module$node_modules$monaco_editor$esm$vs$base$browser$ui$aria$aria.js.map
