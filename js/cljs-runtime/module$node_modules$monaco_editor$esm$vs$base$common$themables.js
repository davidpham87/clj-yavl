shadow$provide.module$node_modules$monaco_editor$esm$vs$base$common$themables = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, ThemeColor:{enumerable:!0, get:function() {
    return ThemeColor;
  }}, ThemeIcon:{enumerable:!0, get:function() {
    return ThemeIcon;
  }}});
  var require$_DOT__SLASH_codicons_DOT_js = require("module$node_modules$monaco_editor$esm$vs$base$common$codicons"), ThemeColor;
  (function(ThemeColor) {
    ThemeColor.isThemeColor = function(obj) {
      return obj && typeof obj === "object" && typeof obj.id === "string";
    };
  })(ThemeColor ||= {});
  var ThemeIcon;
  (function(ThemeIcon) {
    function asClassNameArray(icon) {
      icon = ThemeIconIdRegex.exec(icon.id);
      if (!icon) {
        return asClassNameArray(require$_DOT__SLASH_codicons_DOT_js.Codicon.error);
      }
      const [, id, modifier] = icon;
      icon = ["codicon", "codicon-" + id];
      modifier && icon.push("codicon-modifier-" + modifier.substring(1));
      return icon;
    }
    ThemeIcon.iconNameSegment = "[A-Za-z0-9]+";
    ThemeIcon.iconNameExpression = "[A-Za-z0-9-]+";
    ThemeIcon.iconModifierExpression = "~[A-Za-z]+";
    ThemeIcon.iconNameCharacter = "[A-Za-z0-9~-]";
    const ThemeIconIdRegex = new RegExp(`^(${ThemeIcon.iconNameExpression})(${ThemeIcon.iconModifierExpression})?$`);
    ThemeIcon.asClassNameArray = asClassNameArray;
    ThemeIcon.asClassName = function(icon) {
      return asClassNameArray(icon).join(" ");
    };
    ThemeIcon.asCSSSelector = function(icon) {
      return "." + asClassNameArray(icon).join(".");
    };
    ThemeIcon.isThemeIcon = function(obj) {
      return obj && typeof obj === "object" && typeof obj.id === "string" && (typeof obj.color === "undefined" || ThemeColor.isThemeColor(obj.color));
    };
    const _regexFromString = new RegExp(`^\\$\\((${ThemeIcon.iconNameExpression}(?:${ThemeIcon.iconModifierExpression})?)\\)$`);
    ThemeIcon.fromString = function(str) {
      if (str = _regexFromString.exec(str)) {
        return [, str] = str, {id:str};
      }
    };
    ThemeIcon.fromId = function(id) {
      return {id};
    };
    ThemeIcon.modify = function(icon, modifier) {
      icon = icon.id;
      const tildeIndex = icon.lastIndexOf("~");
      tildeIndex !== -1 && (icon = icon.substring(0, tildeIndex));
      modifier && (icon = `${icon}~${modifier}`);
      return {id:icon};
    };
    ThemeIcon.getModifier = function(icon) {
      const tildeIndex = icon.id.lastIndexOf("~");
      if (tildeIndex !== -1) {
        return icon.id.substring(tildeIndex + 1);
      }
    };
    ThemeIcon.isEqual = function(ti1, ti2) {
      var _a, _b;
      return ti1.id === ti2.id && ((_a = ti1.color) === null || _a === void 0 ? void 0 : _a.id) === ((_b = ti2.color) === null || _b === void 0 ? void 0 : _b.id);
    };
  })(ThemeIcon ||= {});
};

//# sourceMappingURL=module$node_modules$monaco_editor$esm$vs$base$common$themables.js.map
