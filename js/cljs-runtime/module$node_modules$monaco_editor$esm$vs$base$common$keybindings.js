shadow$provide.module$node_modules$monaco_editor$esm$vs$base$common$keybindings = function(require, module, exports) {
  function decodeKeybinding(keybinding, OS) {
    if (typeof keybinding === "number") {
      if (keybinding === 0) {
        return null;
      }
      var firstChord = (keybinding & 65535) >>> 0;
      keybinding = (keybinding & 4294901760) >>> 16;
      return keybinding !== 0 ? new Keybinding([createSimpleKeybinding(firstChord, OS), createSimpleKeybinding(keybinding, OS)]) : new Keybinding([createSimpleKeybinding(firstChord, OS)]);
    }
    firstChord = [];
    for (let i = 0; i < keybinding.length; i++) {
      firstChord.push(createSimpleKeybinding(keybinding[i], OS));
    }
    return new Keybinding(firstChord);
  }
  function createSimpleKeybinding(keybinding, OS) {
    const ctrlCmd = keybinding & 2048 ? !0 : !1, winCtrl = keybinding & 256 ? !0 : !1;
    return new KeyCodeChord(OS === 2 ? winCtrl : ctrlCmd, keybinding & 1024 ? !0 : !1, keybinding & 512 ? !0 : !1, OS === 2 ? ctrlCmd : winCtrl, keybinding & 255);
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, KeyCodeChord:{enumerable:!0, get:function() {
    return KeyCodeChord;
  }}, Keybinding:{enumerable:!0, get:function() {
    return Keybinding;
  }}, ResolvedChord:{enumerable:!0, get:function() {
    return ResolvedChord;
  }}, ResolvedKeybinding:{enumerable:!0, get:function() {
    return ResolvedKeybinding;
  }}, ScanCodeChord:{enumerable:!0, get:function() {
    return ScanCodeChord;
  }}, createSimpleKeybinding:{enumerable:!0, get:function() {
    return createSimpleKeybinding;
  }}, decodeKeybinding:{enumerable:!0, get:function() {
    return decodeKeybinding;
  }}});
  var require$_DOT__SLASH_errors_DOT_js = require("module$node_modules$monaco_editor$esm$vs$base$common$errors");
  class KeyCodeChord {
    constructor(ctrlKey, shiftKey, altKey, metaKey, keyCode) {
      this.ctrlKey = ctrlKey;
      this.shiftKey = shiftKey;
      this.altKey = altKey;
      this.metaKey = metaKey;
      this.keyCode = keyCode;
    }
    equals(other) {
      return other instanceof KeyCodeChord && this.ctrlKey === other.ctrlKey && this.shiftKey === other.shiftKey && this.altKey === other.altKey && this.metaKey === other.metaKey && this.keyCode === other.keyCode;
    }
    isModifierKey() {
      return this.keyCode === 0 || this.keyCode === 5 || this.keyCode === 57 || this.keyCode === 6 || this.keyCode === 4;
    }
    isDuplicateModifierCase() {
      return this.ctrlKey && this.keyCode === 5 || this.shiftKey && this.keyCode === 4 || this.altKey && this.keyCode === 6 || this.metaKey && this.keyCode === 57;
    }
  }
  class ScanCodeChord {
    constructor(ctrlKey, shiftKey, altKey, metaKey, scanCode) {
      this.ctrlKey = ctrlKey;
      this.shiftKey = shiftKey;
      this.altKey = altKey;
      this.metaKey = metaKey;
      this.scanCode = scanCode;
    }
    isDuplicateModifierCase() {
      return this.ctrlKey && (this.scanCode === 157 || this.scanCode === 161) || this.shiftKey && (this.scanCode === 158 || this.scanCode === 162) || this.altKey && (this.scanCode === 159 || this.scanCode === 163) || this.metaKey && (this.scanCode === 160 || this.scanCode === 164);
    }
  }
  class Keybinding {
    constructor(chords) {
      if (chords.length === 0) {
        throw (0,require$_DOT__SLASH_errors_DOT_js.illegalArgument)("chords");
      }
      this.chords = chords;
    }
  }
  class ResolvedChord {
    constructor(ctrlKey, shiftKey, altKey, metaKey, keyLabel, keyAriaLabel) {
      this.ctrlKey = ctrlKey;
      this.shiftKey = shiftKey;
      this.altKey = altKey;
      this.metaKey = metaKey;
      this.keyLabel = keyLabel;
      this.keyAriaLabel = keyAriaLabel;
    }
  }
  class ResolvedKeybinding {
  }
};

//# sourceMappingURL=module$node_modules$monaco_editor$esm$vs$base$common$keybindings.js.map
