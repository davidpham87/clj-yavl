shadow$provide.module$node_modules$monaco_editor$esm$vs$base$common$buffer = function(require, module, exports) {
  function readUInt16LE(source, offset) {
    return source[offset + 0] << 0 >>> 0 | source[offset + 1] << 8 >>> 0;
  }
  function writeUInt16LE(destination, value, offset) {
    destination[offset + 0] = value & 255;
    destination[offset + 1] = value >>> 8 & 255;
  }
  function readUInt32BE(source, offset) {
    return source[offset] * 2 ** 24 + source[offset + 1] * 65536 + source[offset + 2] * 256 + source[offset + 3];
  }
  function writeUInt32BE(destination, value, offset) {
    destination[offset + 3] = value;
    value >>>= 8;
    destination[offset + 2] = value;
    value >>>= 8;
    destination[offset + 1] = value;
    destination[offset] = value >>> 8;
  }
  function readUInt8(source, offset) {
    return source[offset];
  }
  function writeUInt8(destination, value, offset) {
    destination[offset] = value;
  }
  var Buffer = require("module$node_modules$buffer$index").Buffer;
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, VSBuffer:{enumerable:!0, get:function() {
    return VSBuffer;
  }}, readUInt16LE:{enumerable:!0, get:function() {
    return readUInt16LE;
  }}, readUInt32BE:{enumerable:!0, get:function() {
    return readUInt32BE;
  }}, readUInt8:{enumerable:!0, get:function() {
    return readUInt8;
  }}, writeUInt16LE:{enumerable:!0, get:function() {
    return writeUInt16LE;
  }}, writeUInt32BE:{enumerable:!0, get:function() {
    return writeUInt32BE;
  }}, writeUInt8:{enumerable:!0, get:function() {
    return writeUInt8;
  }}});
  require = require("module$node_modules$monaco_editor$esm$vs$base$common$lazy");
  const hasBuffer = typeof Buffer !== "undefined";
  new require.Lazy(() => new Uint8Array(256));
  let textDecoder;
  class VSBuffer {
    static wrap(actual) {
      hasBuffer && !Buffer.isBuffer(actual) && (actual = Buffer.from(actual.buffer, actual.byteOffset, actual.byteLength));
      return new VSBuffer(actual);
    }
    constructor(buffer) {
      this.buffer = buffer;
      this.byteLength = this.buffer.byteLength;
    }
    toString() {
      if (hasBuffer) {
        return this.buffer.toString();
      }
      textDecoder ||= new TextDecoder();
      return textDecoder.decode(this.buffer);
    }
  }
};

//# sourceMappingURL=module$node_modules$monaco_editor$esm$vs$base$common$buffer.js.map
