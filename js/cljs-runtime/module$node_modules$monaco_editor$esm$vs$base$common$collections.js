shadow$provide.module$node_modules$monaco_editor$esm$vs$base$common$collections = function(require, module, exports) {
  function diffSets(before, after) {
    const removed = [], added = [];
    for (const element of before) {
      after.has(element) || removed.push(element);
    }
    for (const element of after) {
      before.has(element) || added.push(element);
    }
    return {removed, added};
  }
  function intersection(setA, setB) {
    const result = new Set();
    for (const elem of setB) {
      setA.has(elem) && result.add(elem);
    }
    return result;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, diffSets:{enumerable:!0, get:function() {
    return diffSets;
  }}, intersection:{enumerable:!0, get:function() {
    return intersection;
  }}});
};

//# sourceMappingURL=module$node_modules$monaco_editor$esm$vs$base$common$collections.js.map
