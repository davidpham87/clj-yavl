shadow$provide.module$node_modules$monaco_editor$esm$vs$base$common$marshalling = function(require, module, exports) {
  function stringify(obj) {
    return JSON.stringify(obj, replacer);
  }
  function parse(text) {
    text = JSON.parse(text);
    return text = revive(text);
  }
  function replacer(key, value) {
    return value instanceof RegExp ? {$mid:2, source:value.source, flags:value.flags} : value;
  }
  function revive(obj, depth = 0) {
    if (!obj || depth > 200) {
      return obj;
    }
    if (typeof obj === "object") {
      switch(obj.$mid) {
        case 1:
          return require$_DOT__SLASH_uri_DOT_js.URI.revive(obj);
        case 2:
          return new RegExp(obj.source, obj.flags);
        case 17:
          return new Date(obj.source);
      }
      if (obj instanceof require$_DOT__SLASH_buffer_DOT_js.VSBuffer || obj instanceof Uint8Array) {
        return obj;
      }
      if (Array.isArray(obj)) {
        for (var i = 0; i < obj.length; ++i) {
          obj[i] = revive(obj[i], depth + 1);
        }
      } else {
        for (i in obj) {
          Object.hasOwnProperty.call(obj, i) && (obj[i] = revive(obj[i], depth + 1));
        }
      }
    }
    return obj;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, parse:{enumerable:!0, get:function() {
    return parse;
  }}, revive:{enumerable:!0, get:function() {
    return revive;
  }}, stringify:{enumerable:!0, get:function() {
    return stringify;
  }}});
  var require$_DOT__SLASH_buffer_DOT_js = require("module$node_modules$monaco_editor$esm$vs$base$common$buffer"), require$_DOT__SLASH_uri_DOT_js = require("module$node_modules$monaco_editor$esm$vs$base$common$uri");
};

//# sourceMappingURL=module$node_modules$monaco_editor$esm$vs$base$common$marshalling.js.map
