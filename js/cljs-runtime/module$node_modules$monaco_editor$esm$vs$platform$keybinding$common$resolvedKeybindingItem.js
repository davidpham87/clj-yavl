shadow$provide.module$node_modules$monaco_editor$esm$vs$platform$keybinding$common$resolvedKeybindingItem = function(require, module, exports) {
  function toEmptyArrayIfContainsNull(arr) {
    const result = [];
    for (let i = 0, len = arr.length; i < len; i++) {
      const element = arr[i];
      if (!element) {
        return [];
      }
      result.push(element);
    }
    return result;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, ResolvedKeybindingItem:{enumerable:!0, get:function() {
    return ResolvedKeybindingItem;
  }}, toEmptyArrayIfContainsNull:{enumerable:!0, get:function() {
    return toEmptyArrayIfContainsNull;
  }}});
  class ResolvedKeybindingItem {
    constructor(resolvedKeybinding, command, commandArgs, when, isDefault, extensionId, isBuiltinExtension) {
      this._resolvedKeybindingItemBrand = void 0;
      this.chords = (this.resolvedKeybinding = resolvedKeybinding) ? toEmptyArrayIfContainsNull(resolvedKeybinding.getDispatchChords()) : [];
      resolvedKeybinding && this.chords.length === 0 && (this.chords = toEmptyArrayIfContainsNull(resolvedKeybinding.getSingleModifierDispatchChords()));
      this.command = (this.bubble = command ? command.charCodeAt(0) === 94 : !1) ? command.substr(1) : command;
      this.commandArgs = commandArgs;
      this.when = when;
      this.isDefault = isDefault;
      this.extensionId = extensionId;
      this.isBuiltinExtension = isBuiltinExtension;
    }
  }
};

//# sourceMappingURL=module$node_modules$monaco_editor$esm$vs$platform$keybinding$common$resolvedKeybindingItem.js.map
