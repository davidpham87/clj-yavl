shadow$provide.module$node_modules$monaco_editor$esm$vs$base$browser$dompurify$dompurify = function(require, module, exports) {
  function unapply(func) {
    return function(thisArg) {
      for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
        args[_key - 1] = arguments[_key];
      }
      return apply(func, thisArg, args);
    };
  }
  function addToSet(set, array, transformCaseFunc) {
    var _transformCaseFunc;
    transformCaseFunc = (_transformCaseFunc = transformCaseFunc) !== null && _transformCaseFunc !== void 0 ? _transformCaseFunc : stringToLowerCase;
    setPrototypeOf && setPrototypeOf(set, null);
    for (_transformCaseFunc = array.length; _transformCaseFunc--;) {
      let element = array[_transformCaseFunc];
      if (typeof element === "string") {
        const lcElement = transformCaseFunc(element);
        lcElement !== element && (isFrozen(array) || (array[_transformCaseFunc] = lcElement), element = lcElement);
      }
      set[element] = !0;
    }
    return set;
  }
  function clone(object) {
    const newObject = create(null);
    for (const [property, value] of entries(object)) {
      newObject[property] = value;
    }
    return newObject;
  }
  function lookupGetter(object, prop) {
    for (; object !== null;) {
      const desc = getOwnPropertyDescriptor(object, prop);
      if (desc) {
        if (desc.get) {
          return unapply(desc.get);
        }
        if (typeof desc.value === "function") {
          return unapply(desc.value);
        }
      }
      object = getPrototypeOf(object);
    }
    return function(element) {
      console.warn("fallback value for", element);
      return null;
    };
  }
  function createDOMPurify() {
    var window$jscomp$0 = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : typeof window === "undefined" ? null : window;
    const DOMPurify = root => createDOMPurify(root);
    DOMPurify.version = "3.0.5";
    DOMPurify.removed = [];
    if (!window$jscomp$0 || !window$jscomp$0.document || window$jscomp$0.document.nodeType !== 9) {
      return DOMPurify.isSupported = !1, DOMPurify;
    }
    const originalDocument = window$jscomp$0.document, currentScript = originalDocument.currentScript;
    let {document} = window$jscomp$0;
    const {DocumentFragment, HTMLTemplateElement, Node, Element, NodeFilter, NamedNodeMap = window$jscomp$0.NamedNodeMap || window$jscomp$0.MozNamedAttrMap, HTMLFormElement, DOMParser, trustedTypes} = window$jscomp$0;
    window$jscomp$0 = Element.prototype;
    const cloneNode = lookupGetter(window$jscomp$0, "cloneNode"), getNextSibling = lookupGetter(window$jscomp$0, "nextSibling"), getChildNodes = lookupGetter(window$jscomp$0, "childNodes"), getParentNode = lookupGetter(window$jscomp$0, "parentNode");
    typeof HTMLTemplateElement === "function" && (window$jscomp$0 = document.createElement("template"), window$jscomp$0.content && window$jscomp$0.content.ownerDocument && (document = window$jscomp$0.content.ownerDocument));
    let trustedTypesPolicy, emptyHTML = "";
    const {implementation, createNodeIterator, createDocumentFragment, getElementsByTagName} = document, {importNode} = originalDocument;
    let hooks = {};
    DOMPurify.isSupported = typeof entries === "function" && typeof getParentNode === "function" && implementation && implementation.createHTMLDocument !== void 0;
    const {MUSTACHE_EXPR, ERB_EXPR, TMPLIT_EXPR, DATA_ATTR, ARIA_ATTR, IS_SCRIPT_OR_DATA, ATTR_WHITESPACE} = EXPRESSIONS;
    let {IS_ALLOWED_URI:IS_ALLOWED_URI$1} = EXPRESSIONS, ALLOWED_TAGS = null;
    const DEFAULT_ALLOWED_TAGS = addToSet({}, [...html$1, ...svg$1, ...svgFilters, ...mathMl$1, ...text]);
    let ALLOWED_ATTR = null;
    const DEFAULT_ALLOWED_ATTR = addToSet({}, [...html, ...svg, ...mathMl, ...xml]);
    let CUSTOM_ELEMENT_HANDLING = Object.seal(Object.create(null, {tagNameCheck:{writable:!0, configurable:!1, enumerable:!0, value:null}, attributeNameCheck:{writable:!0, configurable:!1, enumerable:!0, value:null}, allowCustomizedBuiltInElements:{writable:!0, configurable:!1, enumerable:!0, value:!1}})), FORBID_TAGS = null, FORBID_ATTR = null, ALLOW_ARIA_ATTR = !0, ALLOW_DATA_ATTR = !0, ALLOW_UNKNOWN_PROTOCOLS = !1, ALLOW_SELF_CLOSE_IN_ATTR = !0, SAFE_FOR_TEMPLATES = !1, WHOLE_DOCUMENT = !1, SET_CONFIG = 
    !1, FORCE_BODY = !1, RETURN_DOM = !1, RETURN_DOM_FRAGMENT = !1, RETURN_TRUSTED_TYPE = !1, SANITIZE_DOM = !0, SANITIZE_NAMED_PROPS = !1, KEEP_CONTENT = !0, IN_PLACE = !1, USE_PROFILES = {}, FORBID_CONTENTS = null;
    const DEFAULT_FORBID_CONTENTS = addToSet({}, "annotation-xml audio colgroup desc foreignobject head iframe math mi mn mo ms mtext noembed noframes noscript plaintext script style svg template thead title video xmp".split(" "));
    let DATA_URI_TAGS = null;
    const DEFAULT_DATA_URI_TAGS = addToSet({}, "audio video img source image track".split(" "));
    let URI_SAFE_ATTRIBUTES = null;
    const DEFAULT_URI_SAFE_ATTRIBUTES = addToSet({}, "alt class for id label name pattern placeholder role summary title value style xmlns".split(" "));
    let NAMESPACE = "http://www.w3.org/1999/xhtml", IS_EMPTY_INPUT = !1, ALLOWED_NAMESPACES = null;
    const DEFAULT_ALLOWED_NAMESPACES = addToSet({}, ["http://www.w3.org/1998/Math/MathML", "http://www.w3.org/2000/svg", "http://www.w3.org/1999/xhtml"], stringToString);
    let PARSER_MEDIA_TYPE;
    const SUPPORTED_PARSER_MEDIA_TYPES = ["application/xhtml+xml", "text/html"];
    let transformCaseFunc, CONFIG = null;
    const formElement = document.createElement("form"), isRegexOrFunction = function(testValue) {
      return testValue instanceof RegExp || testValue instanceof Function;
    }, _parseConfig = function(cfg) {
      if (!CONFIG || CONFIG !== cfg) {
        cfg && typeof cfg === "object" || (cfg = {});
        cfg = clone(cfg);
        PARSER_MEDIA_TYPE = SUPPORTED_PARSER_MEDIA_TYPES.indexOf(cfg.PARSER_MEDIA_TYPE) === -1 ? PARSER_MEDIA_TYPE = "text/html" : PARSER_MEDIA_TYPE = cfg.PARSER_MEDIA_TYPE;
        transformCaseFunc = PARSER_MEDIA_TYPE === "application/xhtml+xml" ? stringToString : stringToLowerCase;
        ALLOWED_TAGS = "ALLOWED_TAGS" in cfg ? addToSet({}, cfg.ALLOWED_TAGS, transformCaseFunc) : DEFAULT_ALLOWED_TAGS;
        ALLOWED_ATTR = "ALLOWED_ATTR" in cfg ? addToSet({}, cfg.ALLOWED_ATTR, transformCaseFunc) : DEFAULT_ALLOWED_ATTR;
        ALLOWED_NAMESPACES = "ALLOWED_NAMESPACES" in cfg ? addToSet({}, cfg.ALLOWED_NAMESPACES, stringToString) : DEFAULT_ALLOWED_NAMESPACES;
        URI_SAFE_ATTRIBUTES = "ADD_URI_SAFE_ATTR" in cfg ? addToSet(clone(DEFAULT_URI_SAFE_ATTRIBUTES), cfg.ADD_URI_SAFE_ATTR, transformCaseFunc) : DEFAULT_URI_SAFE_ATTRIBUTES;
        DATA_URI_TAGS = "ADD_DATA_URI_TAGS" in cfg ? addToSet(clone(DEFAULT_DATA_URI_TAGS), cfg.ADD_DATA_URI_TAGS, transformCaseFunc) : DEFAULT_DATA_URI_TAGS;
        FORBID_CONTENTS = "FORBID_CONTENTS" in cfg ? addToSet({}, cfg.FORBID_CONTENTS, transformCaseFunc) : DEFAULT_FORBID_CONTENTS;
        FORBID_TAGS = "FORBID_TAGS" in cfg ? addToSet({}, cfg.FORBID_TAGS, transformCaseFunc) : {};
        FORBID_ATTR = "FORBID_ATTR" in cfg ? addToSet({}, cfg.FORBID_ATTR, transformCaseFunc) : {};
        USE_PROFILES = "USE_PROFILES" in cfg ? cfg.USE_PROFILES : !1;
        ALLOW_ARIA_ATTR = cfg.ALLOW_ARIA_ATTR !== !1;
        ALLOW_DATA_ATTR = cfg.ALLOW_DATA_ATTR !== !1;
        ALLOW_UNKNOWN_PROTOCOLS = cfg.ALLOW_UNKNOWN_PROTOCOLS || !1;
        ALLOW_SELF_CLOSE_IN_ATTR = cfg.ALLOW_SELF_CLOSE_IN_ATTR !== !1;
        SAFE_FOR_TEMPLATES = cfg.SAFE_FOR_TEMPLATES || !1;
        WHOLE_DOCUMENT = cfg.WHOLE_DOCUMENT || !1;
        RETURN_DOM = cfg.RETURN_DOM || !1;
        RETURN_DOM_FRAGMENT = cfg.RETURN_DOM_FRAGMENT || !1;
        RETURN_TRUSTED_TYPE = cfg.RETURN_TRUSTED_TYPE || !1;
        FORCE_BODY = cfg.FORCE_BODY || !1;
        SANITIZE_DOM = cfg.SANITIZE_DOM !== !1;
        SANITIZE_NAMED_PROPS = cfg.SANITIZE_NAMED_PROPS || !1;
        KEEP_CONTENT = cfg.KEEP_CONTENT !== !1;
        IN_PLACE = cfg.IN_PLACE || !1;
        IS_ALLOWED_URI$1 = cfg.ALLOWED_URI_REGEXP || IS_ALLOWED_URI;
        NAMESPACE = cfg.NAMESPACE || "http://www.w3.org/1999/xhtml";
        CUSTOM_ELEMENT_HANDLING = cfg.CUSTOM_ELEMENT_HANDLING || {};
        cfg.CUSTOM_ELEMENT_HANDLING && isRegexOrFunction(cfg.CUSTOM_ELEMENT_HANDLING.tagNameCheck) && (CUSTOM_ELEMENT_HANDLING.tagNameCheck = cfg.CUSTOM_ELEMENT_HANDLING.tagNameCheck);
        cfg.CUSTOM_ELEMENT_HANDLING && isRegexOrFunction(cfg.CUSTOM_ELEMENT_HANDLING.attributeNameCheck) && (CUSTOM_ELEMENT_HANDLING.attributeNameCheck = cfg.CUSTOM_ELEMENT_HANDLING.attributeNameCheck);
        cfg.CUSTOM_ELEMENT_HANDLING && typeof cfg.CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements === "boolean" && (CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements = cfg.CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements);
        SAFE_FOR_TEMPLATES && (ALLOW_DATA_ATTR = !1);
        RETURN_DOM_FRAGMENT && (RETURN_DOM = !0);
        USE_PROFILES && (ALLOWED_TAGS = addToSet({}, [...text]), ALLOWED_ATTR = [], USE_PROFILES.html === !0 && (addToSet(ALLOWED_TAGS, html$1), addToSet(ALLOWED_ATTR, html)), USE_PROFILES.svg === !0 && (addToSet(ALLOWED_TAGS, svg$1), addToSet(ALLOWED_ATTR, svg), addToSet(ALLOWED_ATTR, xml)), USE_PROFILES.svgFilters === !0 && (addToSet(ALLOWED_TAGS, svgFilters), addToSet(ALLOWED_ATTR, svg), addToSet(ALLOWED_ATTR, xml)), USE_PROFILES.mathMl === !0 && (addToSet(ALLOWED_TAGS, mathMl$1), addToSet(ALLOWED_ATTR, 
        mathMl), addToSet(ALLOWED_ATTR, xml)));
        cfg.ADD_TAGS && (ALLOWED_TAGS === DEFAULT_ALLOWED_TAGS && (ALLOWED_TAGS = clone(ALLOWED_TAGS)), addToSet(ALLOWED_TAGS, cfg.ADD_TAGS, transformCaseFunc));
        cfg.ADD_ATTR && (ALLOWED_ATTR === DEFAULT_ALLOWED_ATTR && (ALLOWED_ATTR = clone(ALLOWED_ATTR)), addToSet(ALLOWED_ATTR, cfg.ADD_ATTR, transformCaseFunc));
        cfg.ADD_URI_SAFE_ATTR && addToSet(URI_SAFE_ATTRIBUTES, cfg.ADD_URI_SAFE_ATTR, transformCaseFunc);
        cfg.FORBID_CONTENTS && (FORBID_CONTENTS === DEFAULT_FORBID_CONTENTS && (FORBID_CONTENTS = clone(FORBID_CONTENTS)), addToSet(FORBID_CONTENTS, cfg.FORBID_CONTENTS, transformCaseFunc));
        KEEP_CONTENT && (ALLOWED_TAGS["#text"] = !0);
        WHOLE_DOCUMENT && addToSet(ALLOWED_TAGS, ["html", "head", "body"]);
        ALLOWED_TAGS.table && (addToSet(ALLOWED_TAGS, ["tbody"]), delete FORBID_TAGS.tbody);
        if (cfg.TRUSTED_TYPES_POLICY) {
          if (typeof cfg.TRUSTED_TYPES_POLICY.createHTML !== "function") {
            throw typeErrorCreate('TRUSTED_TYPES_POLICY configuration option must provide a "createHTML" hook.');
          }
          if (typeof cfg.TRUSTED_TYPES_POLICY.createScriptURL !== "function") {
            throw typeErrorCreate('TRUSTED_TYPES_POLICY configuration option must provide a "createScriptURL" hook.');
          }
          trustedTypesPolicy = cfg.TRUSTED_TYPES_POLICY;
          emptyHTML = trustedTypesPolicy.createHTML("");
        } else {
          trustedTypesPolicy === void 0 && (trustedTypesPolicy = _createTrustedTypesPolicy(trustedTypes, currentScript)), trustedTypesPolicy !== null && typeof emptyHTML === "string" && (emptyHTML = trustedTypesPolicy.createHTML(""));
        }
        freeze && freeze(cfg);
        CONFIG = cfg;
      }
    }, MATHML_TEXT_INTEGRATION_POINTS = addToSet({}, ["mi", "mo", "mn", "ms", "mtext"]), HTML_INTEGRATION_POINTS = addToSet({}, ["foreignobject", "desc", "title", "annotation-xml"]), COMMON_SVG_AND_HTML_ELEMENTS = addToSet({}, ["title", "style", "font", "a", "script"]), ALL_SVG_TAGS = addToSet({}, svg$1);
    addToSet(ALL_SVG_TAGS, svgFilters);
    addToSet(ALL_SVG_TAGS, svgDisallowed);
    const ALL_MATHML_TAGS = addToSet({}, mathMl$1);
    addToSet(ALL_MATHML_TAGS, mathMlDisallowed);
    const _forceRemove = function(node) {
      arrayPush(DOMPurify.removed, {element:node});
      try {
        node.parentNode.removeChild(node);
      } catch (_) {
        node.remove();
      }
    }, _removeAttribute = function(name, node) {
      try {
        arrayPush(DOMPurify.removed, {attribute:node.getAttributeNode(name), from:node});
      } catch (_) {
        arrayPush(DOMPurify.removed, {attribute:null, from:node});
      }
      node.removeAttribute(name);
      if (name === "is" && !ALLOWED_ATTR[name]) {
        if (RETURN_DOM || RETURN_DOM_FRAGMENT) {
          try {
            _forceRemove(node);
          } catch (_) {
          }
        } else {
          try {
            node.setAttribute(name, "");
          } catch (_) {
          }
        }
      }
    }, _initDocument = function(dirty) {
      let doc;
      if (FORCE_BODY) {
        dirty = "\x3cremove\x3e\x3c/remove\x3e" + dirty;
      } else {
        var leadingWhitespace = (leadingWhitespace = stringMatch(dirty, /^[\r\n\t ]+/)) && leadingWhitespace[0];
      }
      PARSER_MEDIA_TYPE === "application/xhtml+xml" && NAMESPACE === "http://www.w3.org/1999/xhtml" && (dirty = '\x3chtml xmlns\x3d"http://www.w3.org/1999/xhtml"\x3e\x3chead\x3e\x3c/head\x3e\x3cbody\x3e' + dirty + "\x3c/body\x3e\x3c/html\x3e");
      var dirtyPayload = trustedTypesPolicy ? trustedTypesPolicy.createHTML(dirty) : dirty;
      if (NAMESPACE === "http://www.w3.org/1999/xhtml") {
        try {
          doc = (new DOMParser()).parseFromString(dirtyPayload, PARSER_MEDIA_TYPE);
        } catch (_) {
        }
      }
      if (!doc || !doc.documentElement) {
        doc = implementation.createDocument(NAMESPACE, "template", null);
        try {
          doc.documentElement.innerHTML = IS_EMPTY_INPUT ? emptyHTML : dirtyPayload;
        } catch (_) {
        }
      }
      dirtyPayload = doc.body || doc.documentElement;
      dirty && leadingWhitespace && dirtyPayload.insertBefore(document.createTextNode(leadingWhitespace), dirtyPayload.childNodes[0] || null);
      return NAMESPACE === "http://www.w3.org/1999/xhtml" ? getElementsByTagName.call(doc, WHOLE_DOCUMENT ? "html" : "body")[0] : WHOLE_DOCUMENT ? doc.documentElement : dirtyPayload;
    }, _createIterator = function(root) {
      return createNodeIterator.call(root.ownerDocument || root, root, NodeFilter.SHOW_ELEMENT | NodeFilter.SHOW_COMMENT | NodeFilter.SHOW_TEXT, null, !1);
    }, _isNode = function(object) {
      return typeof Node === "object" ? object instanceof Node : object && typeof object === "object" && typeof object.nodeType === "number" && typeof object.nodeName === "string";
    }, _executeHook = function(entryPoint, currentNode, data) {
      hooks[entryPoint] && arrayForEach(hooks[entryPoint], hook => {
        hook.call(DOMPurify, currentNode, data, CONFIG);
      });
    }, _sanitizeElements = function(currentNode) {
      _executeHook("beforeSanitizeElements", currentNode, null);
      if (currentNode instanceof HTMLFormElement && (typeof currentNode.nodeName !== "string" || typeof currentNode.textContent !== "string" || typeof currentNode.removeChild !== "function" || !(currentNode.attributes instanceof NamedNodeMap) || typeof currentNode.removeAttribute !== "function" || typeof currentNode.setAttribute !== "function" || typeof currentNode.namespaceURI !== "string" || typeof currentNode.insertBefore !== "function" || typeof currentNode.hasChildNodes !== "function")) {
        return _forceRemove(currentNode), !0;
      }
      var content = transformCaseFunc(currentNode.nodeName);
      _executeHook("uponSanitizeElement", currentNode, {tagName:content, allowedTags:ALLOWED_TAGS});
      if (!(!currentNode.hasChildNodes() || _isNode(currentNode.firstElementChild) || _isNode(currentNode.content) && _isNode(currentNode.content.firstElementChild)) && regExpTest(/<[/\w]/g, currentNode.innerHTML) && regExpTest(/<[/\w]/g, currentNode.textContent)) {
        return _forceRemove(currentNode), !0;
      }
      if (!ALLOWED_TAGS[content] || FORBID_TAGS[content]) {
        if (!FORBID_TAGS[content] && content.indexOf("-") > 0 && (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.tagNameCheck, content) || CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.tagNameCheck(content))) {
          return !1;
        }
        if (KEEP_CONTENT && !FORBID_CONTENTS[content]) {
          content = getParentNode(currentNode) || currentNode.parentNode;
          var childNodes = getChildNodes(currentNode) || currentNode.childNodes;
          if (childNodes && content) {
            var childCount = childNodes.length;
            for (--childCount; childCount >= 0; --childCount) {
              content.insertBefore(cloneNode(childNodes[childCount], !0), getNextSibling(currentNode));
            }
          }
        }
        _forceRemove(currentNode);
        return !0;
      }
      if (childNodes = currentNode instanceof Element) {
        (childNodes = getParentNode(currentNode)) && childNodes.tagName || (childNodes = {namespaceURI:NAMESPACE, tagName:"template"});
        childCount = stringToLowerCase(currentNode.tagName);
        const parentTagName = stringToLowerCase(childNodes.tagName);
        childNodes = !(ALLOWED_NAMESPACES[currentNode.namespaceURI] && (currentNode.namespaceURI === "http://www.w3.org/2000/svg" ? childNodes.namespaceURI === "http://www.w3.org/1999/xhtml" ? childCount === "svg" : childNodes.namespaceURI === "http://www.w3.org/1998/Math/MathML" ? childCount === "svg" && (parentTagName === "annotation-xml" || MATHML_TEXT_INTEGRATION_POINTS[parentTagName]) : ALL_SVG_TAGS[childCount] : currentNode.namespaceURI === "http://www.w3.org/1998/Math/MathML" ? childNodes.namespaceURI === 
        "http://www.w3.org/1999/xhtml" ? childCount === "math" : childNodes.namespaceURI === "http://www.w3.org/2000/svg" ? childCount === "math" && HTML_INTEGRATION_POINTS[parentTagName] : ALL_MATHML_TAGS[childCount] : currentNode.namespaceURI === "http://www.w3.org/1999/xhtml" ? childNodes.namespaceURI === "http://www.w3.org/2000/svg" && !HTML_INTEGRATION_POINTS[parentTagName] || childNodes.namespaceURI === "http://www.w3.org/1998/Math/MathML" && !MATHML_TEXT_INTEGRATION_POINTS[parentTagName] ? 
        0 : !ALL_MATHML_TAGS[childCount] && (COMMON_SVG_AND_HTML_ELEMENTS[childCount] || !ALL_SVG_TAGS[childCount]) : PARSER_MEDIA_TYPE === "application/xhtml+xml" && ALLOWED_NAMESPACES[currentNode.namespaceURI]));
      }
      if (childNodes || (content === "noscript" || content === "noembed" || content === "noframes") && regExpTest(/<\/no(script|embed|frames)/i, currentNode.innerHTML)) {
        return _forceRemove(currentNode), !0;
      }
      SAFE_FOR_TEMPLATES && currentNode.nodeType === 3 && (content = currentNode.textContent, content = stringReplace(content, MUSTACHE_EXPR, " "), content = stringReplace(content, ERB_EXPR, " "), content = stringReplace(content, TMPLIT_EXPR, " "), currentNode.textContent !== content && (arrayPush(DOMPurify.removed, {element:currentNode.cloneNode()}), currentNode.textContent = content));
      _executeHook("afterSanitizeElements", currentNode, null);
      return !1;
    }, _isValidAttribute = function(lcTag, lcName, value) {
      if (SANITIZE_DOM && (lcName === "id" || lcName === "name") && (value in document || value in formElement)) {
        return !1;
      }
      if (!ALLOW_DATA_ATTR || FORBID_ATTR[lcName] || !regExpTest(DATA_ATTR, lcName)) {
        if (!ALLOW_ARIA_ATTR || !regExpTest(ARIA_ATTR, lcName)) {
          if (!ALLOWED_ATTR[lcName] || FORBID_ATTR[lcName]) {
            if (!(lcTag.indexOf("-") > 0 && (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.tagNameCheck, lcTag) || CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.tagNameCheck(lcTag)) && (CUSTOM_ELEMENT_HANDLING.attributeNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.attributeNameCheck, lcName) || CUSTOM_ELEMENT_HANDLING.attributeNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.attributeNameCheck(lcName)) || 
            lcName === "is" && CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements && (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.tagNameCheck, value) || CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.tagNameCheck(value)))) {
              return !1;
            }
          } else if (!(URI_SAFE_ATTRIBUTES[lcName] || regExpTest(IS_ALLOWED_URI$1, stringReplace(value, ATTR_WHITESPACE, "")) || (lcName === "src" || lcName === "xlink:href" || lcName === "href") && lcTag !== "script" && stringIndexOf(value, "data:") === 0 && DATA_URI_TAGS[lcTag] || ALLOW_UNKNOWN_PROTOCOLS && !regExpTest(IS_SCRIPT_OR_DATA, stringReplace(value, ATTR_WHITESPACE, ""))) && value) {
            return !1;
          }
        }
      }
      return !0;
    }, _sanitizeAttributes = function(currentNode) {
      let lcName, l;
      _executeHook("beforeSanitizeAttributes", currentNode, null);
      const {attributes} = currentNode;
      if (attributes) {
        var hookEvent = {attrName:"", attrValue:"", keepAttr:!0, allowedAttributes:ALLOWED_ATTR};
        for (l = attributes.length; l--;) {
          var attr = attributes[l];
          const {name, namespaceURI} = attr;
          attr = name === "value" ? attr.value : stringTrim(attr.value);
          lcName = transformCaseFunc(name);
          hookEvent.attrName = lcName;
          hookEvent.attrValue = attr;
          hookEvent.keepAttr = !0;
          hookEvent.forceKeepAttr = void 0;
          _executeHook("uponSanitizeAttribute", currentNode, hookEvent);
          attr = hookEvent.attrValue;
          if (hookEvent.forceKeepAttr) {
            continue;
          }
          _removeAttribute(name, currentNode);
          if (!hookEvent.keepAttr) {
            continue;
          }
          if (!ALLOW_SELF_CLOSE_IN_ATTR && regExpTest(/\/>/i, attr)) {
            _removeAttribute(name, currentNode);
            continue;
          }
          SAFE_FOR_TEMPLATES && (attr = stringReplace(attr, MUSTACHE_EXPR, " "), attr = stringReplace(attr, ERB_EXPR, " "), attr = stringReplace(attr, TMPLIT_EXPR, " "));
          const lcTag = transformCaseFunc(currentNode.nodeName);
          if (_isValidAttribute(lcTag, lcName, attr)) {
            !SANITIZE_NAMED_PROPS || lcName !== "id" && lcName !== "name" || (_removeAttribute(name, currentNode), attr = "user-content-" + attr);
            if (trustedTypesPolicy && typeof trustedTypes === "object" && typeof trustedTypes.getAttributeType === "function" && !namespaceURI) {
              switch(trustedTypes.getAttributeType(lcTag, lcName)) {
                case "TrustedHTML":
                  attr = trustedTypesPolicy.createHTML(attr);
                  break;
                case "TrustedScriptURL":
                  attr = trustedTypesPolicy.createScriptURL(attr);
              }
            }
            try {
              namespaceURI ? currentNode.setAttributeNS(namespaceURI, name, attr) : currentNode.setAttribute(name, attr), arrayPop(DOMPurify.removed);
            } catch (_) {
            }
          }
        }
        _executeHook("afterSanitizeAttributes", currentNode, null);
      }
    }, _sanitizeShadowDOM = function _sanitizeShadowDOM(fragment) {
      let shadowNode;
      const shadowIterator = _createIterator(fragment);
      for (_executeHook("beforeSanitizeShadowDOM", fragment, null); shadowNode = shadowIterator.nextNode();) {
        _executeHook("uponSanitizeShadowNode", shadowNode, null), _sanitizeElements(shadowNode) || (shadowNode.content instanceof DocumentFragment && _sanitizeShadowDOM(shadowNode.content), _sanitizeAttributes(shadowNode));
      }
      _executeHook("afterSanitizeShadowDOM", fragment, null);
    };
    DOMPurify.sanitize = function(dirty) {
      var cfg = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
      let body;
      (IS_EMPTY_INPUT = !dirty) && (dirty = "\x3c!--\x3e");
      if (typeof dirty !== "string" && !_isNode(dirty)) {
        if (typeof dirty.toString === "function") {
          if (dirty = dirty.toString(), typeof dirty !== "string") {
            throw typeErrorCreate("dirty is not a string, aborting");
          }
        } else {
          throw typeErrorCreate("toString is not a function");
        }
      }
      if (!DOMPurify.isSupported) {
        return dirty;
      }
      SET_CONFIG || _parseConfig(cfg);
      DOMPurify.removed = [];
      typeof dirty === "string" && (IN_PLACE = !1);
      if (IN_PLACE) {
        if (dirty.nodeName && (cfg = transformCaseFunc(dirty.nodeName), !ALLOWED_TAGS[cfg] || FORBID_TAGS[cfg])) {
          throw typeErrorCreate("root node is forbidden and cannot be sanitized in-place");
        }
      } else if (dirty instanceof Node) {
        body = _initDocument("\x3c!----\x3e"), cfg = body.ownerDocument.importNode(dirty, !0), cfg.nodeType === 1 && cfg.nodeName === "BODY" ? body = cfg : cfg.nodeName === "HTML" ? body = cfg : body.appendChild(cfg);
      } else {
        if (!RETURN_DOM && !SAFE_FOR_TEMPLATES && !WHOLE_DOCUMENT && dirty.indexOf("\x3c") === -1) {
          return trustedTypesPolicy && RETURN_TRUSTED_TYPE ? trustedTypesPolicy.createHTML(dirty) : dirty;
        }
        body = _initDocument(dirty);
        if (!body) {
          return RETURN_DOM ? null : RETURN_TRUSTED_TYPE ? emptyHTML : "";
        }
      }
      body && FORCE_BODY && _forceRemove(body.firstChild);
      const nodeIterator = _createIterator(IN_PLACE ? dirty : body);
      for (; cfg = nodeIterator.nextNode();) {
        _sanitizeElements(cfg) || (cfg.content instanceof DocumentFragment && _sanitizeShadowDOM(cfg.content), _sanitizeAttributes(cfg));
      }
      if (IN_PLACE) {
        return dirty;
      }
      if (RETURN_DOM) {
        if (RETURN_DOM_FRAGMENT) {
          for (cfg = createDocumentFragment.call(body.ownerDocument); body.firstChild;) {
            cfg.appendChild(body.firstChild);
          }
        } else {
          cfg = body;
        }
        if (ALLOWED_ATTR.shadowroot || ALLOWED_ATTR.shadowrootmode) {
          cfg = importNode.call(originalDocument, cfg, !0);
        }
        return cfg;
      }
      cfg = WHOLE_DOCUMENT ? body.outerHTML : body.innerHTML;
      WHOLE_DOCUMENT && ALLOWED_TAGS["!doctype"] && body.ownerDocument && body.ownerDocument.doctype && body.ownerDocument.doctype.name && regExpTest(DOCTYPE_NAME, body.ownerDocument.doctype.name) && (cfg = "\x3c!DOCTYPE " + body.ownerDocument.doctype.name + "\x3e\n" + cfg);
      SAFE_FOR_TEMPLATES && (cfg = stringReplace(cfg, MUSTACHE_EXPR, " "), cfg = stringReplace(cfg, ERB_EXPR, " "), cfg = stringReplace(cfg, TMPLIT_EXPR, " "));
      return trustedTypesPolicy && RETURN_TRUSTED_TYPE ? trustedTypesPolicy.createHTML(cfg) : cfg;
    };
    DOMPurify.setConfig = function(cfg) {
      _parseConfig(cfg);
      SET_CONFIG = !0;
    };
    DOMPurify.clearConfig = function() {
      CONFIG = null;
      SET_CONFIG = !1;
    };
    DOMPurify.isValidAttribute = function(tag, attr, value) {
      CONFIG || _parseConfig({});
      tag = transformCaseFunc(tag);
      attr = transformCaseFunc(attr);
      return _isValidAttribute(tag, attr, value);
    };
    DOMPurify.addHook = function(entryPoint, hookFunction) {
      typeof hookFunction === "function" && (hooks[entryPoint] = hooks[entryPoint] || [], arrayPush(hooks[entryPoint], hookFunction));
    };
    DOMPurify.removeHook = function(entryPoint) {
      if (hooks[entryPoint]) {
        return arrayPop(hooks[entryPoint]);
      }
    };
    DOMPurify.removeHooks = function(entryPoint) {
      hooks[entryPoint] && (hooks[entryPoint] = []);
    };
    DOMPurify.removeAllHooks = function() {
      hooks = {};
    };
    return DOMPurify;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, addHook:{enumerable:!0, get:function() {
    return addHook;
  }}, clearConfig:{enumerable:!0, get:function() {
    return clearConfig;
  }}, default:{enumerable:!0, get:function() {
    return $$default;
  }}, isSupported:{enumerable:!0, get:function() {
    return isSupported;
  }}, isValidAttribute:{enumerable:!0, get:function() {
    return isValidAttribute;
  }}, removeAllHooks:{enumerable:!0, get:function() {
    return removeAllHooks;
  }}, removeHook:{enumerable:!0, get:function() {
    return removeHook;
  }}, removeHooks:{enumerable:!0, get:function() {
    return removeHooks;
  }}, sanitize:{enumerable:!0, get:function() {
    return sanitize;
  }}, setConfig:{enumerable:!0, get:function() {
    return setConfig;
  }}, version:{enumerable:!0, get:function() {
    return version;
  }}});
  const {entries, setPrototypeOf, isFrozen, getPrototypeOf, getOwnPropertyDescriptor} = Object;
  let {freeze, seal, create} = Object, {apply, construct} = typeof Reflect !== "undefined" && Reflect;
  apply ||= function(fun, thisValue, args) {
    return fun.apply(thisValue, args);
  };
  freeze ||= function(x) {
    return x;
  };
  seal ||= function(x) {
    return x;
  };
  construct ||= function(Func, args) {
    return new Func(...args);
  };
  const arrayForEach = unapply(Array.prototype.forEach), arrayPop = unapply(Array.prototype.pop), arrayPush = unapply(Array.prototype.push), stringToLowerCase = unapply(String.prototype.toLowerCase), stringToString = unapply(String.prototype.toString), stringMatch = unapply(String.prototype.match), stringReplace = unapply(String.prototype.replace), stringIndexOf = unapply(String.prototype.indexOf), stringTrim = unapply(String.prototype.trim), regExpTest = unapply(RegExp.prototype.test), typeErrorCreate = 
  function(func) {
    return function() {
      for (var _len2 = arguments.length, args = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        args[_key2] = arguments[_key2];
      }
      return construct(func, args);
    };
  }(TypeError), html$1 = freeze("a abbr acronym address area article aside audio b bdi bdo big blink blockquote body br button canvas caption center cite code col colgroup content data datalist dd decorator del details dfn dialog dir div dl dt element em fieldset figcaption figure font footer form h1 h2 h3 h4 h5 h6 head header hgroup hr html i img input ins kbd label legend li main map mark marquee menu menuitem meter nav nobr ol optgroup option output p picture pre progress q rp rt ruby s samp section select shadow small source spacer span strike strong style sub summary sup table tbody td template textarea tfoot th thead time tr track tt u ul var video wbr".split(" ")), 
  svg$1 = freeze("svg a altglyph altglyphdef altglyphitem animatecolor animatemotion animatetransform circle clippath defs desc ellipse filter font g glyph glyphref hkern image line lineargradient marker mask metadata mpath path pattern polygon polyline radialgradient rect stop style switch symbol text textpath title tref tspan view vkern".split(" ")), svgFilters = freeze("feBlend feColorMatrix feComponentTransfer feComposite feConvolveMatrix feDiffuseLighting feDisplacementMap feDistantLight feDropShadow feFlood feFuncA feFuncB feFuncG feFuncR feGaussianBlur feImage feMerge feMergeNode feMorphology feOffset fePointLight feSpecularLighting feSpotLight feTile feTurbulence".split(" ")), 
  svgDisallowed = freeze("animate color-profile cursor discard font-face font-face-format font-face-name font-face-src font-face-uri foreignobject hatch hatchpath mesh meshgradient meshpatch meshrow missing-glyph script set solidcolor unknown use".split(" ")), mathMl$1 = freeze("math menclose merror mfenced mfrac mglyph mi mlabeledtr mmultiscripts mn mo mover mpadded mphantom mroot mrow ms mspace msqrt mstyle msub msup msubsup mtable mtd mtext mtr munder munderover mprescripts".split(" ")), mathMlDisallowed = 
  freeze("maction maligngroup malignmark mlongdiv mscarries mscarry msgroup mstack msline msrow semantics annotation annotation-xml mprescripts none".split(" ")), text = freeze(["#text"]), html = freeze("accept action align alt autocapitalize autocomplete autopictureinpicture autoplay background bgcolor border capture cellpadding cellspacing checked cite class clear color cols colspan controls controlslist coords crossorigin datetime decoding default dir disabled disablepictureinpicture disableremoteplayback download draggable enctype enterkeyhint face for headers height hidden high href hreflang id inputmode integrity ismap kind label lang list loading loop low max maxlength media method min minlength multiple muted name nonce noshade novalidate nowrap open optimum pattern placeholder playsinline poster preload pubdate radiogroup readonly rel required rev reversed role rows rowspan spellcheck scope selected shape size sizes span srclang start src srcset step style summary tabindex title translate type usemap valign value width xmlns slot".split(" ")), 
  svg = freeze("accent-height accumulate additive alignment-baseline ascent attributename attributetype azimuth basefrequency baseline-shift begin bias by class clip clippathunits clip-path clip-rule color color-interpolation color-interpolation-filters color-profile color-rendering cx cy d dx dy diffuseconstant direction display divisor dur edgemode elevation end fill fill-opacity fill-rule filter filterunits flood-color flood-opacity font-family font-size font-size-adjust font-stretch font-style font-variant font-weight fx fy g1 g2 glyph-name glyphref gradientunits gradienttransform height href id image-rendering in in2 k k1 k2 k3 k4 kerning keypoints keysplines keytimes lang lengthadjust letter-spacing kernelmatrix kernelunitlength lighting-color local marker-end marker-mid marker-start markerheight markerunits markerwidth maskcontentunits maskunits max mask media method mode min name numoctaves offset operator opacity order orient orientation origin overflow paint-order path pathlength patterncontentunits patterntransform patternunits points preservealpha preserveaspectratio primitiveunits r rx ry radius refx refy repeatcount repeatdur restart result rotate scale seed shape-rendering specularconstant specularexponent spreadmethod startoffset stddeviation stitchtiles stop-color stop-opacity stroke-dasharray stroke-dashoffset stroke-linecap stroke-linejoin stroke-miterlimit stroke-opacity stroke stroke-width style surfacescale systemlanguage tabindex targetx targety transform transform-origin text-anchor text-decoration text-rendering textlength type u1 u2 unicode values viewbox visibility version vert-adv-y vert-origin-x vert-origin-y width word-spacing wrap writing-mode xchannelselector ychannelselector x x1 x2 xmlns y y1 y2 z zoomandpan".split(" ")), 
  mathMl = freeze("accent accentunder align bevelled close columnsalign columnlines columnspan denomalign depth dir display displaystyle encoding fence frame height href id largeop length linethickness lspace lquote mathbackground mathcolor mathsize mathvariant maxsize minsize movablelimits notation numalign open rowalign rowlines rowspacing rowspan rspace rquote scriptlevel scriptminsize scriptsizemultiplier selection separator separators stretchy subscriptshift supscriptshift symmetric voffset width xmlns".split(" ")), 
  xml = freeze(["xlink:href", "xml:id", "xlink:title", "xml:space", "xmlns:xlink"]);
  require = seal(/\{\{[\w\W]*|[\w\W]*\}\}/gm);
  module = seal(/<%[\w\W]*|[\w\W]*%>/gm);
  exports = seal(/\${[\w\W]*}/gm);
  const DATA_ATTR = seal(/^data-[\-\w.\u00B7-\uFFFF]/), ARIA_ATTR = seal(/^aria-[\-\w]+$/), IS_ALLOWED_URI = seal(/^(?:(?:(?:f|ht)tps?|mailto|tel|callto|sms|cid|xmpp):|[^a-z]|[a-z+.\-]+(?:[^a-z+.\-:]|$))/i), IS_SCRIPT_OR_DATA = seal(/^(?:\w+script|data):/i), ATTR_WHITESPACE = seal(/[\u0000-\u0020\u00A0\u1680\u180E\u2000-\u2029\u205F\u3000]/g), DOCTYPE_NAME = seal(/^html$/i);
  var EXPRESSIONS = Object.freeze({__proto__:null, MUSTACHE_EXPR:require, ERB_EXPR:module, TMPLIT_EXPR:exports, DATA_ATTR, ARIA_ATTR, IS_ALLOWED_URI, IS_SCRIPT_OR_DATA, ATTR_WHITESPACE, DOCTYPE_NAME});
  const _createTrustedTypesPolicy = function(trustedTypes, purifyHostElement) {
    if (typeof trustedTypes !== "object" || typeof trustedTypes.createPolicy !== "function") {
      return null;
    }
    let suffix = null;
    purifyHostElement && purifyHostElement.hasAttribute("data-tt-policy-suffix") && (suffix = purifyHostElement.getAttribute("data-tt-policy-suffix"));
    purifyHostElement = "dompurify" + (suffix ? "#" + suffix : "");
    try {
      return trustedTypes.createPolicy(purifyHostElement, {createHTML(html) {
        return html;
      }, createScriptURL(scriptUrl) {
        return scriptUrl;
      }});
    } catch (_) {
      return console.warn("TrustedTypes policy " + purifyHostElement + " could not be created."), null;
    }
  }, $$default = require = createDOMPurify(), version = require.version, isSupported = require.isSupported, sanitize = require.sanitize, setConfig = require.setConfig, clearConfig = require.clearConfig, isValidAttribute = require.isValidAttribute, addHook = require.addHook, removeHook = require.removeHook, removeHooks = require.removeHooks, removeAllHooks = require.removeAllHooks;
};

//# sourceMappingURL=module$node_modules$monaco_editor$esm$vs$base$browser$dompurify$dompurify.js.map
