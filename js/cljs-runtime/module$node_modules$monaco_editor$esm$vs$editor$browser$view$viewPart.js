shadow$provide.module$node_modules$monaco_editor$esm$vs$editor$browser$view$viewPart = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, PartFingerprints:{enumerable:!0, get:function() {
    return PartFingerprints;
  }}, ViewPart:{enumerable:!0, get:function() {
    return ViewPart;
  }}});
  require = require("module$node_modules$monaco_editor$esm$vs$editor$common$viewEventHandler");
  class ViewPart extends require.ViewEventHandler {
    constructor(context) {
      super();
      this._context = context;
      this._context.addEventHandler(this);
    }
    dispose() {
      this._context.removeEventHandler(this);
      super.dispose();
    }
  }
  class PartFingerprints {
    static write(target, partId) {
      target.setAttribute("data-mprt", String(partId));
    }
    static read(target) {
      target = target.getAttribute("data-mprt");
      return target === null ? 0 : parseInt(target, 10);
    }
    static collect(child, stopAt) {
      const result = [];
      let resultLen = 0;
      for (; child && child !== child.ownerDocument.body && child !== stopAt;) {
        child.nodeType === child.ELEMENT_NODE && (result[resultLen++] = this.read(child)), child = child.parentElement;
      }
      child = new Uint8Array(resultLen);
      for (stopAt = 0; stopAt < resultLen; stopAt++) {
        child[stopAt] = result[resultLen - stopAt - 1];
      }
      return child;
    }
  }
};

//# sourceMappingURL=module$node_modules$monaco_editor$esm$vs$editor$browser$view$viewPart.js.map
