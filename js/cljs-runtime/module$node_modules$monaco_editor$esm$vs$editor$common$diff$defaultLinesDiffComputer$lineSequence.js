shadow$provide.module$node_modules$monaco_editor$esm$vs$editor$common$diff$defaultLinesDiffComputer$lineSequence = function(require, module, exports) {
  function getIndentation(str) {
    let i = 0;
    for (; i < str.length && (str.charCodeAt(i) === 32 || str.charCodeAt(i) === 9);) {
      i++;
    }
    return i;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, LineSequence:{enumerable:!0, get:function() {
    return LineSequence;
  }}});
  class LineSequence {
    constructor(trimmedHash, lines) {
      this.trimmedHash = trimmedHash;
      this.lines = lines;
    }
    getElement(offset) {
      return this.trimmedHash[offset];
    }
    get length() {
      return this.trimmedHash.length;
    }
    getBoundaryScore(length) {
      const indentationBefore = length === 0 ? 0 : getIndentation(this.lines[length - 1]);
      length = length === this.lines.length ? 0 : getIndentation(this.lines[length]);
      return 1000 - (indentationBefore + length);
    }
    getText(range) {
      return this.lines.slice(range.start, range.endExclusive).join("\n");
    }
    isStronglyEqual(offset1, offset2) {
      return this.lines[offset1] === this.lines[offset2];
    }
  }
};

//# sourceMappingURL=module$node_modules$monaco_editor$esm$vs$editor$common$diff$defaultLinesDiffComputer$lineSequence.js.map
