shadow$provide.module$node_modules$monaco_editor$esm$vs$editor$common$cursorCommon = function(require, module, exports) {
  function isQuote(ch) {
    return ch === "'" || ch === '"' || ch === "`";
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, CursorConfiguration:{enumerable:!0, get:function() {
    return CursorConfiguration;
  }}, CursorState:{enumerable:!0, get:function() {
    return CursorState;
  }}, EditOperationResult:{enumerable:!0, get:function() {
    return EditOperationResult;
  }}, PartialModelCursorState:{enumerable:!0, get:function() {
    return PartialModelCursorState;
  }}, PartialViewCursorState:{enumerable:!0, get:function() {
    return PartialViewCursorState;
  }}, SingleCursorState:{enumerable:!0, get:function() {
    return SingleCursorState;
  }}, isQuote:{enumerable:!0, get:function() {
    return isQuote;
  }}});
  var require$_DOT__SLASH_core_SLASH_position_DOT_js = require("module$node_modules$monaco_editor$esm$vs$editor$common$core$position"), require$_DOT__SLASH_core_SLASH_range_DOT_js = require("module$node_modules$monaco_editor$esm$vs$editor$common$core$range"), require$_DOT__SLASH_core_SLASH_selection_DOT_js = require("module$node_modules$monaco_editor$esm$vs$editor$common$core$selection"), require$_DOT__SLASH_languages_SLASH_supports_DOT_js = require("module$node_modules$monaco_editor$esm$vs$editor$common$languages$supports"), 
  require$_DOT__SLASH_core_SLASH_cursorColumns_DOT_js = require("module$node_modules$monaco_editor$esm$vs$editor$common$core$cursorColumns"), require$_DOT__SLASH_core_SLASH_indentation_DOT_js = require("module$node_modules$monaco_editor$esm$vs$editor$common$core$indentation");
  const autoCloseAlways = () => !0, autoCloseNever = () => !1, autoCloseBeforeWhitespace = chr => chr === " " || chr === "\t";
  class CursorConfiguration {
    static shouldRecreate(e) {
      return e.hasChanged(143) || e.hasChanged(129) || e.hasChanged(37) || e.hasChanged(76) || e.hasChanged(78) || e.hasChanged(79) || e.hasChanged(6) || e.hasChanged(7) || e.hasChanged(11) || e.hasChanged(9) || e.hasChanged(10) || e.hasChanged(14) || e.hasChanged(127) || e.hasChanged(50) || e.hasChanged(90);
    }
    constructor(languageId, modelOptions, configuration, languageConfigurationService) {
      var _a;
      this.languageConfigurationService = languageConfigurationService;
      this._cursorMoveConfigurationBrand = void 0;
      this._languageId = languageId;
      configuration = configuration.options;
      languageConfigurationService = configuration.get(143);
      const fontInfo = configuration.get(50);
      this.readOnly = configuration.get(90);
      this.tabSize = modelOptions.tabSize;
      this.indentSize = modelOptions.indentSize;
      this.insertSpaces = modelOptions.insertSpaces;
      this.stickyTabStops = configuration.get(115);
      this.lineHeight = fontInfo.lineHeight;
      this.typicalHalfwidthCharacterWidth = fontInfo.typicalHalfwidthCharacterWidth;
      this.pageSize = Math.max(1, Math.floor(languageConfigurationService.height / this.lineHeight) - 2);
      this.useTabStops = configuration.get(127);
      this.wordSeparators = configuration.get(129);
      this.emptySelectionClipboard = configuration.get(37);
      this.copyWithSyntaxHighlighting = configuration.get(25);
      this.multiCursorMergeOverlapping = configuration.get(76);
      this.multiCursorPaste = configuration.get(78);
      this.multiCursorLimit = configuration.get(79);
      this.autoClosingBrackets = configuration.get(6);
      this.autoClosingComments = configuration.get(7);
      this.autoClosingQuotes = configuration.get(11);
      this.autoClosingDelete = configuration.get(9);
      this.autoClosingOvertype = configuration.get(10);
      this.autoSurround = configuration.get(14);
      this.autoIndent = configuration.get(12);
      this.surroundingPairs = {};
      this._electricChars = null;
      this.shouldAutoCloseBefore = {quote:this._getShouldAutoClose(languageId, this.autoClosingQuotes, !0), comment:this._getShouldAutoClose(languageId, this.autoClosingComments, !1), bracket:this._getShouldAutoClose(languageId, this.autoClosingBrackets, !1)};
      this.autoClosingPairs = this.languageConfigurationService.getLanguageConfiguration(languageId).getAutoClosingPairs();
      if (modelOptions = this.languageConfigurationService.getLanguageConfiguration(languageId).getSurroundingPairs()) {
        for (const pair of modelOptions) {
          this.surroundingPairs[pair.open] = pair.close;
        }
      }
      languageId = this.languageConfigurationService.getLanguageConfiguration(languageId).comments;
      this.blockCommentStartToken = (_a = languageId === null || languageId === void 0 ? void 0 : languageId.blockCommentStartToken) !== null && _a !== void 0 ? _a : null;
    }
    get electricChars() {
      var _a;
      if (!this._electricChars) {
        this._electricChars = {};
        const electricChars = (_a = this.languageConfigurationService.getLanguageConfiguration(this._languageId).electricCharacter) === null || _a === void 0 ? void 0 : _a.getElectricCharacters();
        if (electricChars) {
          for (const char of electricChars) {
            this._electricChars[char] = !0;
          }
        }
      }
      return this._electricChars;
    }
    onElectricCharacter(character, context, column) {
      context = (0,require$_DOT__SLASH_languages_SLASH_supports_DOT_js.createScopedLineTokens)(context, column - 1);
      const electricCharacterSupport = this.languageConfigurationService.getLanguageConfiguration(context.languageId).electricCharacter;
      return electricCharacterSupport ? electricCharacterSupport.onElectricCharacter(character, context, column - context.firstCharOffset) : null;
    }
    normalizeIndentation(str) {
      return (0,require$_DOT__SLASH_core_SLASH_indentation_DOT_js.normalizeIndentation)(str, this.indentSize, this.insertSpaces);
    }
    _getShouldAutoClose(languageId, autoCloseConfig, forQuotes) {
      switch(autoCloseConfig) {
        case "beforeWhitespace":
          return autoCloseBeforeWhitespace;
        case "languageDefined":
          return this._getLanguageDefinedShouldAutoClose(languageId, forQuotes);
        case "always":
          return autoCloseAlways;
        case "never":
          return autoCloseNever;
      }
    }
    _getLanguageDefinedShouldAutoClose(languageId, forQuotes) {
      const autoCloseBeforeSet = this.languageConfigurationService.getLanguageConfiguration(languageId).getAutoCloseBeforeSet(forQuotes);
      return c => autoCloseBeforeSet.indexOf(c) !== -1;
    }
    visibleColumnFromColumn(model, position) {
      return require$_DOT__SLASH_core_SLASH_cursorColumns_DOT_js.CursorColumns.visibleColumnFromColumn(model.getLineContent(position.lineNumber), position.column, this.tabSize);
    }
    columnFromVisibleColumn(model, lineNumber, visibleColumn) {
      visibleColumn = require$_DOT__SLASH_core_SLASH_cursorColumns_DOT_js.CursorColumns.columnFromVisibleColumn(model.getLineContent(lineNumber), visibleColumn, this.tabSize);
      const minColumn = model.getLineMinColumn(lineNumber);
      if (visibleColumn < minColumn) {
        return minColumn;
      }
      model = model.getLineMaxColumn(lineNumber);
      return visibleColumn > model ? model : visibleColumn;
    }
  }
  class CursorState {
    static fromModelState(modelState) {
      return new PartialModelCursorState(modelState);
    }
    static fromViewState(viewState) {
      return new PartialViewCursorState(viewState);
    }
    static fromModelSelection(modelSelection) {
      modelSelection = require$_DOT__SLASH_core_SLASH_selection_DOT_js.Selection.liftSelection(modelSelection);
      modelSelection = new SingleCursorState(require$_DOT__SLASH_core_SLASH_range_DOT_js.Range.fromPositions(modelSelection.getSelectionStart()), 0, 0, modelSelection.getPosition(), 0);
      return CursorState.fromModelState(modelSelection);
    }
    static fromModelSelections(modelSelections) {
      const states = [];
      for (let i = 0, len = modelSelections.length; i < len; i++) {
        states[i] = this.fromModelSelection(modelSelections[i]);
      }
      return states;
    }
    constructor(modelState, viewState) {
      this._cursorStateBrand = void 0;
      this.modelState = modelState;
      this.viewState = viewState;
    }
    equals(other) {
      return this.viewState.equals(other.viewState) && this.modelState.equals(other.modelState);
    }
  }
  class PartialModelCursorState {
    constructor(modelState) {
      this.modelState = modelState;
      this.viewState = null;
    }
  }
  class PartialViewCursorState {
    constructor(viewState) {
      this.modelState = null;
      this.viewState = viewState;
    }
  }
  class SingleCursorState {
    constructor(selectionStart, selectionStartKind, selectionStartLeftoverVisibleColumns, position, leftoverVisibleColumns) {
      this.selectionStart = selectionStart;
      this.selectionStartKind = selectionStartKind;
      this.selectionStartLeftoverVisibleColumns = selectionStartLeftoverVisibleColumns;
      this.position = position;
      this.leftoverVisibleColumns = leftoverVisibleColumns;
      this._singleCursorStateBrand = void 0;
      this.selection = SingleCursorState._computeSelection(this.selectionStart, this.position);
    }
    equals(other) {
      return this.selectionStartLeftoverVisibleColumns === other.selectionStartLeftoverVisibleColumns && this.leftoverVisibleColumns === other.leftoverVisibleColumns && this.selectionStartKind === other.selectionStartKind && this.position.equals(other.position) && this.selectionStart.equalsRange(other.selectionStart);
    }
    hasSelection() {
      return !this.selection.isEmpty() || !this.selectionStart.isEmpty();
    }
    move(inSelectionMode, lineNumber, column, leftoverVisibleColumns) {
      return inSelectionMode ? new SingleCursorState(this.selectionStart, this.selectionStartKind, this.selectionStartLeftoverVisibleColumns, new require$_DOT__SLASH_core_SLASH_position_DOT_js.Position(lineNumber, column), leftoverVisibleColumns) : new SingleCursorState(new require$_DOT__SLASH_core_SLASH_range_DOT_js.Range(lineNumber, column, lineNumber, column), 0, leftoverVisibleColumns, new require$_DOT__SLASH_core_SLASH_position_DOT_js.Position(lineNumber, column), leftoverVisibleColumns);
    }
    static _computeSelection(selectionStart, position) {
      return selectionStart.isEmpty() || !position.isBeforeOrEqual(selectionStart.getStartPosition()) ? require$_DOT__SLASH_core_SLASH_selection_DOT_js.Selection.fromPositions(selectionStart.getStartPosition(), position) : require$_DOT__SLASH_core_SLASH_selection_DOT_js.Selection.fromPositions(selectionStart.getEndPosition(), position);
    }
  }
  class EditOperationResult {
    constructor(type, commands, opts) {
      this._editOperationResultBrand = void 0;
      this.type = type;
      this.commands = commands;
      this.shouldPushStackElementBefore = opts.shouldPushStackElementBefore;
      this.shouldPushStackElementAfter = opts.shouldPushStackElementAfter;
    }
  }
};

//# sourceMappingURL=module$node_modules$monaco_editor$esm$vs$editor$common$cursorCommon.js.map
