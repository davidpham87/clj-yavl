shadow$provide.module$node_modules$monaco_editor$esm$vs$base$common$range = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, Range:{enumerable:!0, get:function() {
    return Range;
  }}});
  var Range;
  (function(Range) {
    function intersect(one, other) {
      if (one.start >= other.end || other.start >= one.end) {
        return {start:0, end:0};
      }
      const start = Math.max(one.start, other.start);
      one = Math.min(one.end, other.end);
      return one - start <= 0 ? {start:0, end:0} : {start, end:one};
    }
    function isEmpty(range) {
      return range.end - range.start <= 0;
    }
    Range.intersect = intersect;
    Range.isEmpty = isEmpty;
    Range.intersects = function(one, other) {
      return !isEmpty(intersect(one, other));
    };
    Range.relativeComplement = function(one, other) {
      const result = [], first = {start:one.start, end:Math.min(other.start, one.end)};
      one = {start:Math.max(other.end, one.start), end:one.end};
      isEmpty(first) || result.push(first);
      isEmpty(one) || result.push(one);
      return result;
    };
  })(Range ||= {});
};

//# sourceMappingURL=module$node_modules$monaco_editor$esm$vs$base$common$range.js.map
